// Barrel intentionally empty — import server functions directly from their files:
// e.g. import { getNearbyMerchants } from '@/server/merchants.functions'
// This avoids barrel-import pitfalls (bundle size, circular deps, poor tree-shaking).
